This folder is for static assets like images, fonts, and icons.

Any file placed in here can be referenced directly from the root URL.
For example, if you upload `logo.png` here, you can reference it in your code as `/logo.png` or set `VITE_TM_LOGO_URL=/logo.png` in your environment variables.
